---
---

<section class="section-sertifikasi">
  <div class="container">
    
    <h2>🏷️ Apa Itu Sertifikasi Hijau dan Mengapa Diperlukan?</h2>

    <p class="intro">
      <strong>Sertifikasi hijau</strong> adalah bukti resmi bahwa sumber kayu berasal dari hutan yang dikelola secara 
      berkelanjutan dan tidak terkait dengan pembalakan liar.
    </p>

    <!-- JENIS SERTIFIKASI -->
    <h3 class="sub-title">Jenis Sertifikasi yang Umum</h3>
    <div class="cert-grid">
      <div class="cert-card">
        <span class="icon">🌳</span>
        <h4>FSC</h4>
        <p>Diakui secara internasional untuk memastikan pengelolaan hutan yang bertanggung jawab.</p>
      </div>
      <div class="cert-card">
        <span class="icon">🌲</span>
        <h4>PEFC</h4>
        <p>Mempromosikan praktik kehutanan berkelanjutan dengan standar global.</p>
      </div>
      <div class="cert-card">
        <span class="icon">🇮🇩</span>
        <h4>SVLK</h4>
        <p>Sertifikasi resmi Indonesia dan menjadi <strong>syarat wajib</strong> ekspor kayu.</p>
      </div>
    </div>

    <!-- MENGAPA PENTING -->
    <h3 class="sub-title">Mengapa Sertifikasi Ini Penting?</h3>
    <div class="timeline">
      <div class="step">
        <span class="dot"></span>
        <p>🛡️ Menghindari penolakan oleh negara tujuan ekspor.</p>
      </div>
      <div class="step">
        <span class="dot"></span>
        <p>🚢 Memperlancar proses pengiriman dan perizinan.</p>
      </div>
      <div class="step">
        <span class="dot"></span>
        <p>🏆 Meningkatkan kredibilitas perusahaan di pasar global.</p>
      </div>
    </div>

    <p class="bridge">
      Jika sertifikasi sudah terpenuhi, langkah berikutnya adalah menerapkan strategi menyeluruh agar ekspor tetap berkelanjutan.
    </p>

  </div>
</section>

<style>
  :root {
    --dark-green: #0f3d2e;
    --light-bg: #f4f9f6;
    --border: rgba(15, 61, 46, 0.25);
  }

  .section-sertifikasi {
    padding: 80px 20px;
    background: var(--light-bg);
  }

  .container {
    max-width: 960px;
    margin: auto;
  }

  h2 {
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    color: var(--dark-green);
    margin-bottom: 18px;
  }

  .intro {
    text-align: center;
    font-size: 1.1rem;
    line-height: 1.7;
    max-width: 720px;
    margin: 0 auto 50px;
    opacity: 0.9;
  }

  .sub-title {
    font-size: 1.45rem;
    font-weight: 600;
    color: var(--dark-green);
    margin: 40px 0 20px;
    text-align: center;
  }

  /* GRID CARD */
  .cert-grid {
    display: grid;
    gap: 22px;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    margin-bottom: 50px;
  }

  .cert-card {
    background: #fff;
    padding: 28px;
    border-radius: 12px;
    border: 1px solid var(--border);
    text-align: center;
    transition: transform .25s ease, box-shadow .25s ease;
  }

  .cert-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 28px rgba(0,0,0,0.09);
  }

  .cert-card .icon {
    display: block;
    font-size: 2rem;
    margin-bottom: 10px;
  }

  .cert-card h4 {
    font-size: 1.2rem;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--dark-green);
  }

  .cert-card p {
    font-size: 0.98rem;
    line-height: 1.6;
    opacity: 0.9;
  }

  /* TIMELINE LIST */
  .timeline {
    border-left: 3px solid var(--dark-green);
    padding-left: 18px;
    max-width: 720px;
    margin: 0 auto 40px;
  }

  .step {
    position: relative;
    margin-bottom: 18px;
    padding-left: 10px;
  }

  .step .dot {
    width: 12px;
    height: 12px;
    background: var(--dark-green);
    border-radius: 50%;
    position: absolute;
    left: -25px;
    top: 5px;
  }

  .bridge {
    text-align: center;
    font-size: 1.05rem;
    opacity: 0.85;
  }

  @media(max-width: 768px) {
    h2 { font-size: 1.75rem; }
    .timeline { border-left-width: 2px; }
  }
</style>
